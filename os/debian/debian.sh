# =============================================================================
# debian.sh — Debian/Ubuntu-specific configuration
# =============================================================================

# Add Debian-specific configuration here.
# This file is sourced on Debian-based systems (Ubuntu, Mint, Pop!_OS, etc.)

# Example: apt update shortcut
# alias aptup='sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y'
